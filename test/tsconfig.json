{
  "$schema": "https://json.schemastore.org/tsconfig.json",
  "extends": "../tsconfig.json",
  "include": ["**/*.ts"],
  "exclude": ["temp"],
  "compilerOptions": {
    "types": ["vitest/globals"]
  }
}