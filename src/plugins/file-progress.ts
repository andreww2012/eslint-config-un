import {definePluginMetadata} from './shared';

export default definePluginMetadata('file-progress', {
  configs: ['fileProgress'],
  docsUrl: 'https://github.com/sibiraj-s/eslint-plugin-file-progress/blob/HEAD/README.md',
  rules: {},
});
