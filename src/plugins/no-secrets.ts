import {definePluginMetadata} from './shared';

export default definePluginMetadata('no-secrets', {
  configs: ['noSecrets'],
  docsUrl: 'https://github.com/nickdeis/eslint-plugin-no-secrets/blob/HEAD/README.md',
  rules: {},
});
