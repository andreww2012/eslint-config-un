import {definePluginMetadata} from './shared';

export default definePluginMetadata('header', {
  configs: ['header'],
  docsUrl: 'https://github.com/Stuk/eslint-plugin-header/blob/HEAD/README.md',
  rules: {},
});
