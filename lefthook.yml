pre-push:
  parallel: true
  commands:
    eslint:
      run: npx eslint {push_files}
    prettier:
      run: npx prettier --check --ignore-unknown {push_files}
    spellcheck:
      run: npx cspell --exclude '**/.*' --no-must-find-files {push_files}
    knip:
      run: nr knip
    types:
      run: nr lint:types:go
    build:
      run: nr build:code:test
commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {0}
