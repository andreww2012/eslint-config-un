pre-push:
  parallel: true
  commands:
    eslint:
      glob: '*.{js,jsx,cjs,mjs,ts,tsx,cts,mts,vue}'
      run: npx eslint {push_files}
    prettier:
      glob: '*.{js,jsx,cjs,mjs,ts,tsx,cts,mts,vue}'
      run: npx prettier --check {push_files}
    spellcheck:
      run: npx cspell --exclude '**/.*' --no-must-find-files {push_files}
