---
'eslint-config-un': minor
---

Added a new config `githubActions` which uses [`eslint-plugin-github-action`](https://npmx.dev/eslint-plugin-github-action), ❓ enabled if `.github/workflows` directory exists