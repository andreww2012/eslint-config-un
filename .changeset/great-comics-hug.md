---
'eslint-config-un': minor
---

Added a new config `githubActions` which uses [`eslint-plugin-github-action`](https://npmjs.com/eslint-plugin-github-action), ❓ enabled if `.github/workflows` directory exists