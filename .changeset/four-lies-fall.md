---
'eslint-config-un': minor
---

markdown/sentencesPerLine: ignore `LICENSE.md` file in addition to the `ignores` provided by the parent config or explicitly.
Added `ignoresAdditional` option to control if this `ignores` entry should be added.