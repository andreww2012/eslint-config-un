---
'eslint-config-un': minor
---

packageJson: added a new option, `publishable`, to control whether additional rules meant for publishable `package.json`s should be turned on.
You can also now specify multiple `packageJson` configs
