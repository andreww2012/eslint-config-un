---
'eslint-config-un': minor
---

[**BREAKING**] Added a new config `disablePrettierIncompatibleRules` which is ❓ enabled if `prettier` package is installed and completely replaces the removed `disablePrettierIncompatibleRules` root option
