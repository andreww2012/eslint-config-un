---
'eslint-config-un': minor
---

Added a new config `jestDom` which uses [`eslint-plugin-jest-dom`](https://npmjs.com/eslint-plugin-jest-dom), ❓ enabled automatically if `@testing-library/jest-dom` package is installed
