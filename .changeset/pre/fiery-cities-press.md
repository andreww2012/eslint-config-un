---
'eslint-config-un': patch
---

githubActions: default `<job>.steps.uses` style was changed from less secure `'release'` to `'commit'` (affects [`github-actions/prefer-step-uses-style`](https://eslint-plugin-github-action.ntnyq.com/rules/prefer-step-uses-style) rule)
