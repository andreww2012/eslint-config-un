---
'eslint-config-un': patch
---

sonar: changed the default value of `testsRules` option to `false` because some rules enabled by this option produced non-high quality reports compared to the respective testing libraries' plugins
