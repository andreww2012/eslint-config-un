---
'eslint-config-un': patch
---

svelte: added a separate `setup` sub-config which configures parser for `.svelte` and `.svelte.{js,ts}` files and is completely independent from the main `svelte` config