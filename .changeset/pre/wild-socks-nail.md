---
'eslint-config-un': minor
---

[**BREAKING**] toml, yaml: renamed `doNotIgnoreFilesByDefault` to `ignoresAdditional` and enhanced its type (added the ability to set `false` and require the full pattern string to be specified instead of just the file name).