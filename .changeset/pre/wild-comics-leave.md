---
'eslint-config-un': minor
---

Added a new config `jestDom` which uses [`eslint-plugin-jest-dom`](https://npmx.dev/eslint-plugin-jest-dom), ❓ enabled automatically if `@testing-library/jest-dom` package is installed
