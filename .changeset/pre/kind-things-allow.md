---
'eslint-config-un': patch
---

vue: applying config with default parameters on .vue files no longer crashes if Vue version cannot be determined. Additionally, now a console warning is printed if this happens
