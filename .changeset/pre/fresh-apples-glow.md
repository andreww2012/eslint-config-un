---
'eslint-config-un': minor
---

angular: added a new sub-config `template/html` which uses [`@html-eslint/eslint-plugin-angular-template`](https://npmx.dev/@html-eslint/eslint-plugin-angular-template), ❓ enabled if Angular version is at least 18 and `template` sub-config is not disabled