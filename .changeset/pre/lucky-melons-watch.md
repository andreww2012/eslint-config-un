---
'eslint-config-un': minor
---

[**BREAKING**] un: `allow` option now accepts an object instead of an array, and `undefined` is now allowed by default, and the provided value will be merged with the default
