---
"eslint-config-un": minor
---

css: added a new sub-config `scss` which parses `.scss` files via the syntax provided by [`@humanwhocodes/scsstree`](https://npmx.dev/@humanwhocodes/scsstree) and applies the same [`@eslint/css`](https://npmx.dev/@eslint/css) rules