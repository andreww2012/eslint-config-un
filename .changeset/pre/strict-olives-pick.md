---
'eslint-config-un': minor
---

Added a new config `lockfile` which uses [`eslint-plugin-lockfile`](https://npmx.dev/eslint-plugin-lockfile), ❓ enabled if `defaultConfigsStatus` is set to `misc-enabled`