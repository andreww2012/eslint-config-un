---
'eslint-config-un': minor
---

User provided `ignores` are now merged with the internal `ignores`, which primarily exist to ignore the files that the current config is not supposed to work with and might crash on
